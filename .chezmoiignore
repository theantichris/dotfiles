# Ignore if not Linux
{{ if ne .chezmoi.os "linux" }}
.zshrc
aerospace.toml
karabiner.json
{{ end }}

# Ignore if not MacOS
{{ if ne .chezmoi.os "darwin" }}
.zshrc
aerospace.toml
karabiner.json
{{ end }}

# Ignore if not Windows
{{ if ne .chezmoi.os "windows" }}
profile.ps1
{{ end }}
