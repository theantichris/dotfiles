# Ignore if not Linux or MacOS
{{ if (and(ne .chezmoi.os "linux") (ne .chezmoi.darwin)) }}
.zshrc
aerospace.toml
karabiner.json
{{end}}

# Ignore if not Windows
{{ if ne .chezmoi.os "windows" }}
profile.ps1
{{ end }}
